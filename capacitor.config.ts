import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'company-users',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
