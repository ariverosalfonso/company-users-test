export interface Appointment {
  date: any;
  veterinarian: string;
  pet: string;
  novedades: string;
  reason: string;
  totalPrice: string;
  state: string;
}

export interface AppointmentState {
  estado: string;
}
